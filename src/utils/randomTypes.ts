export type ModifiedUserData = {
  id: number;
  name: string;
  gender: string;
  dateOfBirth: string;
  email: string;
};

export interface linkArray {
  href: string;
  label: string;
  svgD?: string;
}
